version: '3.8'

services:
  convert2svg:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./outputs:/app/outputs
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
